
// Recursion h

// Declaracion de funciones


long unsigned int factorial(long unsigned int numero);  // funcion general
long double potencia(long double numero, int n);
int mcd(int a, int b);   // mcd(a,b)=mcd(b%a,a)
int mcm(int a, int b);

double f_(int n, float a, float b, float c);  // f_0=1  f_1=-1  f_2=2  f_n=a*f_(n-3)+b*f_(n-2)+c*f(n-1)  para n>2
int sumar_elem_arreglo(int* arreglo,int dimension);    // Partiendo en dos el arreglo
double minimo_elem_arreglo(int* arreglo,int dimension);
